setopt    extendedhistory   # Save start and duration timestamps to history
setopt    histignorealldups # Only remember one instance of each line

HISTFILE=$HOME/.zhistory
HISTSIZE=10500
SAVEHIST=10000

# Grep the history with 'h'
h () { history 0 | grep $1 }
