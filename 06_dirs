unsetopt  listambiguous   # Don't require an extra tab before completion list
setopt    nolistbeep      # Don't beep when listing completions
setopt    nobanghist      # Allow ! inside strings

alias ..='cd ..'
